interface:
  display_name: "I18n Delivery Pipeline"
  short_description: "Route PRD translation and i18n delivery"
  default_prompt: "Use $i18n-delivery-pipeline to decide whether this PRD or spec request needs full-document translation or a localization delivery package, then run that path."
